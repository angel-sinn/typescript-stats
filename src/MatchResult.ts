// fix set of possible outcomes
// enum - enumeration -> signal to others that this is a collection of closely related values known at2 compile time
export enum MatchResult {
  HomeWin = 'H',
  AwayWin = 'A',
  Draw = 'D',
}
